{
  "name": "webapp",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp public/static/app.js dist/static/app.js && cp public/static/styles.css dist/static/styles.css && cp public/static/logo.png dist/static/logo.png",
    "preview": "wrangler pages dev",
    "deploy": "npm run build && wrangler pages deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@hono/vite-cloudflare-pages": "^0.4.3",
    "hono": "^4.12.5"
  },
  "devDependencies": {
    "@hono/vite-build": "^1.2.0",
    "@hono/vite-dev-server": "^0.18.2",
    "browserify": "^17.0.1",
    "qrcode": "^1.5.4",
    "vite": "^6.3.5",
    "wrangler": "^4.4.0"
  }
}
